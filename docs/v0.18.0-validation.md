# RoonIA v0.18.0 Validation

Date: 2026-07-20
Status: release automated validation complete; deployment and post-deployment
live checks not requested.

## Release identity

- Intended branch: `main`
- Intended tag: `v0.18.0`
- Package version: `0.18.0`
- Application version: `0.18.0`
- Source: promotion of the validated `beta` contents over v0.17.3, followed by
  the release metadata and documentation commit.

The complete release suite and the architecture-specific evidence are the
acceptance basis for the tag. Deployment and live checks remain separate from
this repository promotion.

## Preserved contracts

- HTTP routes, payloads, authentication and homogeneous errors.
- Portal routes, assets, presentation and update workflow.
- MCP tool names, schemas, annotations, envelopes and widget resource URIs.
- Roon verification, ambiguity handling and safe-volume behavior.
- SQLite schema, playlist identity and persisted data formats.

Detailed structural scope and focused evidence are recorded in
[v0.18.0 Architecture Validation](v0.18.0-architecture-validation.md).

## Automated validation

Release commands executed from the repository root:

```powershell
pnpm run test
pnpm run build
git diff --check
```

Results:

- `pnpm run test`: 213 tests passed, 0 failed, 0 skipped.
- `pnpm run build`: TypeScript compilation passed.
- The canonical stdio MCP entry compiled to
  `dist/bridge-v2/mcp/index.js`.
- `git diff --check`: no whitespace errors.
- No LXC deployment, restart or audible Roon operation was performed.

## Release acceptance

The final release commit must contain the exact `0.18.0` package and application
versions. The annotated `v0.18.0` tag must point to that same commit after it is
pushed to `main`. The `beta` branch can then advance independently to `0.18.1`.
