# RoonIA v0.17.1 Validation

Date: 2026-07-16
Status: automated validation complete; deployment and live validation pending.

## Release identity

- Intended branch: `main`
- Intended tag: `v0.17.1`
- Package version: `0.17.1`
- Application version: `0.17.1`
- Source: fast-forward promotion of the current `beta` history over v0.17.0,
  followed by the release metadata and documentation commit.

The release must not be tagged until the promoted commit is deployed and the
live checks below pass.

## Included scope

- Deterministic media search and typed result grouping.
- Richer artist discographies and complete multi-disc album details.
- Strict recording resolution for playlist association and playback.
- Playlist artwork, paging and recent-playback ordering.
- Portal music-library UX improvements.
- Cache-busted v17 widgets with signed artwork routing.

Detailed behavior is covered by
[Deterministic Search Validation](deterministic-search-validation.md),
[Strict Track Resolution Validation](strict-track-resolution-validation.md),
[Music UX Validation](music-ux-validation.md) and
[Widget v17 Artwork Routing Validation](widget-v17-validation.md).

## Automated validation

Run from the repository root:

```powershell
pnpm run test
pnpm run build
git diff --check
git diff --stat
git status --short
```

Results on 2026-07-16:

- `pnpm run test`: passed, 136 tests, 136 passed, 0 failed. The command includes
  a TypeScript compilation before the Node test suite.
- `pnpm run build`: passed (`tsc`).
- Package, application and automated version assertions report `0.17.1`.

## Pending live validation

After an explicit deployment request:

1. Confirm the deployed commit matches `origin/main` and the container build.
2. Confirm `package.json`, `/version` and MCP initialization report `0.17.1`.
3. Confirm `/roon/status` reports core, transport and browse ready.
4. Inspect `tools/list` for the expected 34 tools, schemas, annotations and v17
   widget resource URIs.
5. Exercise deterministic search, album/artist navigation, playlist resolution
   and artwork loading through the same MCP tools ChatGPT calls.
6. Follow the repository audible-test safety rules if playback is required and
   verify the final zone and queue state.

## Tagging gate

Automated validation is complete. Live validation and approval for the
annotated `v0.17.1` tag remain pending.
