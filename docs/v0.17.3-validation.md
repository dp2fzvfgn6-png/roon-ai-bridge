# RoonIA v0.17.3 Validation

Date: 2026-07-20
Status: release automated validation complete; focused beta and local visual
validation complete; deployment and post-deployment live checks not requested.

## Release identity

- Intended branch: `main`
- Intended tag: `v0.17.3`
- Package version: `0.17.3`
- Application version: `0.17.3`
- Source: fast-forward promotion of the validated `beta` history over v0.17.2,
  followed by the release metadata and documentation commit.

The accumulated beta evidence and the complete release test and build runs are
the acceptance basis for the tag. Deployment and post-deployment live checks
remain separate and are not part of this repository promotion.

## Release scope

- Expiring temporary playlists with strict recording preflight, isolated
  listing, cleanup and in-place promotion.
- Ordered pagination for tracks in selected native Roon catalog playlists.
- Correct installed-channel identity while changing future update preferences.
- Six focused read-only v19 widgets, including playlist-library, queue and zone
  views with private artwork hydration.
- Normalized queue metadata shared by the portal and MCP views.
- Compact persistent player-bar controls for zones, grouped outputs and native
  volume steps.

Detailed behavior and focused evidence are recorded in:

- [v0.17.3 Temporary Playlists Validation](v0.17.3-temporary-playlists-validation.md)
- [v0.17.3 Player Bar Validation](v0.17.3-player-bar-validation.md)
- [Widget v19 Library, Queue and Zones Validation](widget-v19-validation.md)

## Automated validation

Release commands executed from the repository root:

```powershell
pnpm run test
pnpm run build
git diff --check
```

Results:

- `pnpm run test`: 214 tests passed, 0 failed.
- `pnpm run build`: TypeScript compilation passed.
- `git diff --check`: no whitespace errors.
- No LXC deployment, restart or audible Roon operation was performed.

## Release acceptance

The final release commit must contain the exact `0.17.3` package and application
versions. The annotated `v0.17.3` tag must point to that same commit after it is
pushed to `main`. The `beta` branch can then advance independently to `0.18.0`.
